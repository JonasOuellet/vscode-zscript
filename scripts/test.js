var command = require("./genCommandIndex");

console.log(command.setLineMaxCharLength("The special Button name ??? indentifies this as a Macro. If the zscript text file is placed in the ZStartup\Macros folder, ZBrush will automatically load the zscript; the file name will show as the Button name.", 80));